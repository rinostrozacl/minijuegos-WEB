runtime: nodejs18

instance_class: F1

env_variables:
  HOST: "0.0.0.0"
  PORT: "8080"
  NODE_ENV: "production"

handlers:
  - url: /.*
    script: auto
    secure: always

build_env_variables:
  GOOGLE_NODE_RUN_SCRIPTS: "build"
